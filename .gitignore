# Python
__pycache__/
*.pyc

# ── Private local data — NEVER commit/share these ──────────────────────────
# Your real account size, journal, watchlist, and approve/reject marks.
data/settings.json
data/trades.json
data/watchlist.json
data/status.json
data/uploads/

# Per-user workspaces created on the server (one folder per friend).
data/users/

# Regenerable price cache (rebuilt from Yahoo; large).
data/cache/

# Coach-only generated markdown (regenerated from JSON locally).
watchlist.md
journal/trades.md

# On-demand demo data — friends run this scan themselves (kept out to avoid shipping a stale sample).
data/suspicious.json

# NOTE: these ARE committed as a WARM SNAPSHOT (shipped by make-build) so the hosted site shows the
# SAME graded setups as local the instant it boots — the server re-grades + re-sizes per user on each
# read, and suggestions.json is sanitized of the owner's per-user sizing before shipping:
#   data/suggestions.json, data/market.json, data/sector_heat.json, data/news.json
# Plus the universe definition: data/screeners.json, data/universe.json, data/themes.json, data/sectors.json
